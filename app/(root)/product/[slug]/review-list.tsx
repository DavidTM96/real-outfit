"use client";

const ReviewList = ({
  userId,
  productId,
  productSlug,
}: {
  userId: string;
  productId: string;
  productSlug: string;
}) => {
  console.log(userId, productId, productSlug);

  return <>Review List</>;
};

export default ReviewList;
